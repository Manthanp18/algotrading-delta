{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node server-live.js BTCUSD 50000 $PORT",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}